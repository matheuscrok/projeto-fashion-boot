spring:
  datasource:
    url: jdbc:mysql://172.17.0.2:3306/fashionboot?createDatabaseIfNotExist=true
    username: root
    password: root
#    data: classpath:data.sql
#    schema: classpath:data.sql
#    initialization-mode: always
    platform: mysql
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8081

